import {Container} from "#src/shared/ui";

const HeroSection = () => (
	<section className="h-screen">
		<Container>
			<div className="flex items-center justify-center h-full">
				<h1 className="text-4xl md:text-5xl xl:text-6xl">FOSSFINDER</h1>
			</div>
		</Container>
	</section>
);

export default HeroSection;
