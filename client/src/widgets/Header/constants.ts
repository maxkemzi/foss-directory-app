import {Pathname} from "#src/shared/constants";

const navbarItems = [
	{id: 1, name: "Home", pathname: Pathname.HOME},
	{id: 2, name: "Projects", pathname: Pathname.PROJECTS}
];

export {navbarItems};
