import {Header} from "#src/widgets/Header";
import {HeroSection} from "#src/widgets/HeroSection";

const Home = () => (
	<>
		<Header isAbsolute />
		<main>
			<HeroSection />
		</main>
	</>
);

export default Home;
