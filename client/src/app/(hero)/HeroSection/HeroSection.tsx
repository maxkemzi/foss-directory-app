import {Container} from "#src/components/ui";

const HeroSection = () => {
	return (
		<section className="h-screen">
			<Container>
				<div className="flex items-center justify-center h-full">
					<h1 className="text-6xl">FOSSFINDER</h1>
				</div>
			</Container>
		</section>
	);
};

export default HeroSection;
